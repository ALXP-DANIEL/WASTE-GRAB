# shared-types

Shared WasteGrab API and domain contracts used by the frontend and backend.

## Running unit tests

Run `nx test shared-types` to execute the unit tests via [Vitest](https://vitest.dev/).
